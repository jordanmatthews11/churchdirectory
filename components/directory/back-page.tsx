'use client'

/** Blank final page for the printed directory. */
export function BackPage() {
  return (
    <section className="directory-page directory-back-page break-after-page" aria-label="Blank back page" />
  )
}
